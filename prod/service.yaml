apiVersion: apps/v1
kind: Deployment
metadata:
  name: comment-service-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: comment-service-pod
  template:
    metadata:
      labels:
        app: comment-service-pod
    spec:
      serviceAccountName: comment-service-account
      containers:
      - name: comment-service-container
        image: gcr.io/phading-prod/comment-service:latest
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readiness
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
---
apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: comment-service-monitoring
spec:
  selector:
    matchLabels:
      app: comment-service-pod
  endpoints:
  - port: 8080
    path: /metricsz
    interval: 30s
---
apiVersion: v1
kind: Service
metadata:
  name: comment-service
spec:
  selector:
    app: comment-service-pod
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: comment-service-lb-health-check
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        port: 8080
        requestPath: /healthz
  targetRef:
    group: ""
    kind: Service
    name: comment-service
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: comment-service-route-external
spec:
  parentRefs:
  - name: phading-gateway-external
    sectionName: https
  hostnames:
  - www.secount.com
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /ctw
    backendRefs:
    - name: comment-service
      port: 8080
